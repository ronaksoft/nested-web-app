(function () {
  'use strict';

  angular
    .module('ronak.nested.web.components')
    .controller('contactsController', contactsController);

  /** @ngInject */
  function contactsController($stateParams, toastr, $uibModal, $state, $timeout, $q, $scope) {
    var vm = this;

  }
})();
